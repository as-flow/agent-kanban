REPOS_DIRECTORY=/path/to/your/repos

# Terminal emulator to use for task windows.
# Supported: ghostty (default), kitty, alacritty, wezterm, iterm2, terminal
TERMINAL_APP=ghostty

# Optional: override the terminal binary path (auto-detected if empty)
# TERMINAL_PATH=/usr/local/bin/ghostty
